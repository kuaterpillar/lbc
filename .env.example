## Exemple de fichier .env (NE PAS commiter de secrets)
## Copie ce fichier en ".env" puis remplace les valeurs.

# Proxy (optionnel mais recommandé pour éviter les blocages)
# PROXY_HOST="proxy.example.com"
# PROXY_PORT=8080
